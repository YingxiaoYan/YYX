---
title: "Some functions"
subtitle: "a package in development"
author: "Yingxiao Yan"
date: last-modified
date-format: "DD-MMM-YYYY"
params:
  name: setosa
format: 
  html:
    theme: pulse
    highlight: kate
    toc:  true  ## automaticlly generate table of contente
    toc-depth: 4  ## specify the number of section levels to include in the table of contents
    toc-location: left  ##default right
    smooth-scroll:  true  ### when you click on the table of content, how did what you see move
    number-sections: true  ## add number in the session or not
    number-depth: 4
    title-block-banner: true  ## add a banner for the quarto
    code-fold: true  ## fold the code
    code-tools: true   ## a bar for code 
    code-copy: true  ##   adds a copy icon in the code chunk and
    code-overflow: wrap  ## when the code lines are too wide for container
    df-print: paged  ## kable is another option How the dataframe looks like 
    standalone: false  ### specifies if all assets and libraries must be integrated into the output html file as a standalone document.
    fig-align: right
description: >
  This is
  a multiline
  string
execute:
  freeze: true

---
# Section 1: Aim & intro

## Aim

The aim of what I do is to provide some convenience function regarding calculating running time, memory usage and treating error for my other packages([MUVR](https://gitlab.com/CarlBrunius/MUVR/-/tree/MUVR2?ref_type=heads),[TriplotGUI](https://gitlab.com/YingxiaoYan/triplotgui))

## Some description for exisiting function used in examples 

MUVR::MUVR(): Perform variable selection and prediction in repeated nested cross-validation 

TriplotGUI::TriplotGUIshiny(): The function that Open my shiny app that do some stuff(doesn't matter here)

## functions in this package
- time_evaluation()
- memory
- ignore_loop_error()
- supporting function: str_paste0(), find_identical_strings


*This is italic text*

**This is bold text**

- Point one
- Point two

H~2~O

2~10~

2^10^

$2^{10}$  

$2_{10}$

$\sum\limits_{n=1}^{10} \frac{3}{2}\cdot n$

> This is a block quote. This
> paragraph has two linessssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssssss.


> 1. This is a list inside a block quote. ![This is a caption](298442286864228942.jpg){width=10%} <!--This comment is ignore by the quarto, html style. The figure file need to be in the same folder as the qmd file-->
> 2. Second item. [this](https://quarto.org)

Let's explore the **iris** dataset.

```   <!--Text block code-->
This is generic block code.
```

# Section 2: Time calulation
## table and figure
```{r}
#| label: tbl-iris
#| tbl-cap: !expr paste0("The column names are",paste(colnames(iris),collapse=", "))

## add name to the figure
head(iris)
```



```{r}
#| eval: false
#| echo: true 
## echo display the chunk
## eval truth is default whne false, not run the code
ggplot(dfr4,aes(x=Month,y=fraction,colour=Year,group=Year))+
  geom_point(size=2)+
  geom_line()+
  labs(x="Month",y="Fraction of support issues")+
  scale_colour_manual(values=c("#000000","#E69F00","#56B4E9",
  "#009E73","#F0E442","#006699","#D55E00","#CC79A7"))+
  theme_bw(base_size=12,base_family="Gidole")+
  theme(panel.border=element_blank(),
        panel.grid.minor=element_blank(),
        panel.grid.major.x=element_blank(),
        axis.ticks=element_blank())
```

# Section 3: memory calculation
## plot
```{r}
#| label: fig-plot-a
#| fig-cap: This is a figure caption.
#| fig-height: 6
#| fig-width: 6
## fig-cap is to add name to the figure
plot(x=iris$Petal.Length,y=iris$Petal.Width)
```

# Section 4: ignoring error
```
quarto::quarto_render("quarto_try.qmd")
quarto::quarto_render("quarto_try.qmd")
```
```
If change to pdf
format: pdf
pdf-engine: pdflatex
```
